services:
  offtube:
    build:
      context: .
    restart: unless-stopped
    networks:
      - offtube
    volumes:
      - /mnt/offtube:/data
    environment:
      - CLEANUP_ENABLED=true

networks:
  offtube:
